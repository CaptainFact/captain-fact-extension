doctype html

html
  head
    meta(charset='UTF-8')
    title CaptainFact
    style.
      body { width: 500px; min-height: 30px; }

  body
    #root
    script(src= ['staging', 'prod'].includes(env) ? '/js/popup.bundle.js' : 'http://localhost:3000/js/popup.bundle.js')
